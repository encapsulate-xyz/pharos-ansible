---
# maintains testnet inventory grouped by project names
all:
  vars:
    env: testnet
  children:
    pharos:
      hosts:
        ts.validator.pharos.testnet.encapsulate.xyz:
          type: validator
